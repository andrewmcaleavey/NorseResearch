% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilityFuncs.R
\name{collapse_versioned_columns}
\alias{collapse_versioned_columns}
\title{Collapse columns that contain numeric version suffixes}
\usage{
collapse_versioned_columns(
  dat,
  suffix_pattern = "_\\\\d+$",
  conflict = c("highest_suffix", "error"),
  coerce = c("common", "none"),
  base_names = NULL
)
}
\arguments{
\item{dat}{A data frame or tibble.}

\item{suffix_pattern}{Regular expression matching the numeric suffix at the
end of a column name. The default matches \verb{_1}, \verb{_2}, and so on. Custom
patterns such as \code{"_A\\\\d+$"} are supported; the matched names must still
end in digits.}

\item{conflict}{Conflict policy when more than one version is non-missing in
a row. \code{"highest_suffix"} (the default) keeps the value from the highest
numeric suffix. \code{"error"} stops and reports the affected rows.}

\item{coerce}{Type coercion policy. With \code{"common"} (the default),
numeric-like character values are converted to numeric and otherwise
participating values are converted to character. Date/POSIX values are
preserved when all participating columns have the same class. With
\code{"none"}, incompatible participating column types cause an error.}

\item{base_names}{Optional character vector restricting which base names are
collapsed. By default every base identified by \code{suffix_pattern} is used.}
}
\value{
\code{dat} with the selected suffixed columns collapsed into their base
names. If all values in a row are missing, the result is missing; its type
follows the common-type rules described under \code{coerce}.
}
\description{
Collapse columns such as \code{Q140}, \code{Q140_1}, and \code{Q140_10} into the unsuffixed
base column. Numeric suffixes are ordered numerically, so \verb{_10} is preferred
to \verb{_2} when both contain a value. The unsuffixed column, when present, is
treated as the lowest-priority version.
}
\examples{
dat <- data.frame(
  Q140 = c(NA, 2, 3),
  Q140_2 = c(2, NA, NA),
  Q140_10 = c(10, NA, NA)
)
collapse_versioned_columns(dat)$Q140
}
