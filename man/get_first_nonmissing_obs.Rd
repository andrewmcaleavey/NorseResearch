% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleaningFuncs.R
\name{get_first_nonmissing_obs}
\alias{get_first_nonmissing_obs}
\title{Pick out the first non-missing observation per patient}
\usage{
get_first_nonmissing_obs(
  .data,
  id = "respondent id",
  vars = NULL,
  require = c("any", "all")
)
}
\arguments{
\item{.data}{Data set on which to select rows.}

\item{id}{Name of the variable representing the patient ID. It may be
supplied quoted or unquoted, and may contain spaces.}

\item{vars}{Character vector of columns used to identify a usable
observation. Defaults to every column except \code{id}.}

\item{require}{Whether a usable row must contain a non-missing value in
\code{"any"} or \code{"all"} of \code{vars}. The default is \code{"any"}.}
}
\value{
A tibble containing at most one row per ID. IDs with no qualifying
row are omitted. Rows are considered in their existing input order; sort
the data first if “first” should mean chronological order.
}
\description{
Pick out the first non-missing observation per patient
}
\examples{
dat <- data.frame(
  `respondent id` = c("a", "a", "b"),
  score = c(NA, 2, NA),
  stringsAsFactors = FALSE,
  check.names = FALSE
)
get_first_nonmissing_obs(dat, "respondent id", vars = "score")
}
