test_that("multiplication works", {
  expect_equal(2 * 2, 4)
})

# score_all() function
#' is there only 1 version?
#' if no, combine variables and clean the names
#'
#' score variables with case_when()
#' e.g., anger should be NA when version is 2.x, but should exist for 3
#'
#'
